<UserControl x:Class="FrostLive.Controls.VolumeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200"
             Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Ползунок громкости -->
        <Border Grid.Column="1" 
               Background="#2200FFFF"
               BorderBrush="#4400FFFF"
               BorderThickness="1"
               CornerRadius="4"
               Padding="3"
               VerticalAlignment="Center"
               Margin="0,0,15,0">
            <Grid>
                <Slider x:Name="VolumeSlider"
                        Minimum="0" 
                        Maximum="100" 
                        Value="{Binding Volume, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        VerticalAlignment="Center"
                        Style="{StaticResource NeonCustomSlider}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>