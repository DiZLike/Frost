<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrostLive"
        xmlns:Controls="clr-namespace:FrostLive.Controls" x:Class="FrostLive.MainWindow"
        mc:Ignorable="d"
        Title="FrostLive - Retro Pub.radio" 
        Height="40" Width="1000"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#FF0a0a0f" Icon="/play-button_icon-icons.com_60615.png">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="611*"/>
            <ColumnDefinition Width="389*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="20,20,20,20" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column -->
            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Player Control -->
                <Controls:PlayerControl Grid.Row="0"
                    CurrentSong="{Binding CurrentSong}"
                    PlayerStatus="{Binding PlayerStatus}"
                    IsPlaying="{Binding IsPlaying}"
                    Volume="{Binding Volume, Mode=TwoWay}"
                    CurrentTime="{Binding CurrentTime}"
                    PlayPauseCommand="{Binding PlayPauseCommand}"
                    PlayPauseButtonText="{Binding PlayPauseButtonText}"
                    LeftPeakValue="{Binding LeftPeakValue}"
                    RightPeakValue="{Binding RightPeakValue}"/>

                <!-- New Tracks Control - привязан к низу -->
                <Controls:TrackListControl Grid.Row="1"
                    Title="NEW TRACKS"
                    Tracks="{Binding NewTracks}"
                    DownloadCommand="{Binding DownloadTrackCommand}"
                    ShowRefreshButton="False"
                    VerticalAlignment="Bottom"/>
            </Grid>

            <!-- Right Column - History -->
            <Controls:TrackListControl Grid.Column="1" 
                                      Title="PLAY HISTORY"
                                      Tracks="{Binding HistoryTracks}"
                                      DownloadCommand="{Binding DownloadTrackCommand}"
                                      RefreshCommand="{Binding RefreshHistoryCommand}"
                                      ShowRefreshButton="True"/>
        </Grid>
    </Grid>
</Window>